{% extends 'admin/base.html.twig' %}

{% block content %}
    <h1>Supprimer un article</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a href="{{ path('homeAdmin') }}">Dashboard</a></li>
        <li class="breadcrumb-item"><a href="{{ path('postsAdmin') }}">Articles</a></li>
        <li class="breadcrumb-item active">Modifier un article</li>
    </ol>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}
    {{ form_row(form.author) }}
    {{ form_row(form.img) }}
    {{ form_row(form.category) }}
    {{ form_row(form.categoryNew) }}
    {{ form_row(form.keywords) }}
    {{ form_row(form.keywordsNew) }}
    <a href="#" id="add_category" class="btn btn-success">Ajouter un mot-clé</a>
    {{ form_end(form) }}
{% endblock %}