{% extends 'admin/base.html.twig' %}

{% block content %}
    <h1>Modifier le mot de passe d'un utilisateur</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a href="{{ path('homeAdmin') }}">Dashboard</a></li>
        <li class="breadcrumb-item"><a href="{{ path('usersAdmin') }}">Utilisateurs</a></li>
        <li class="breadcrumb-item active">Modifier le mot de passe d'un utilisateur</li>
    </ol>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}