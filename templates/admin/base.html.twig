<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>Comètes Administration</title>
    {{ encore_entry_link_tags('admin') }}
    <link href="https://cdn.datatables.net/1.10.20/css/dataTables.bootstrap4.min.css" rel="stylesheet" crossorigin="anonymous" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/js/all.min.js" crossorigin="anonymous"></script>
</head>
<body class="sb-nav-fixed">
<nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('homeAdmin') }}">Comètes Administration</a>
    <!-- Navbar Search-->
    <form class="d-none d-md-inline-block form-inline ml-auto mr-0 mr-md-3 my-2 my-md-0">
        <div class="input-group">
            <div class="input-group">
                <button class="btn btn-info" onclick="window.open('{{path('home')}}');return false" type="button">Retour au site</button>
            </div>
        </div>
    </form>
    <!-- Navbar-->
    <ul class="navbar-nav ml-auto ml-md-0">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" id="userDropdown" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fas fa-user fa-fw"></i></a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                <a class="dropdown-item" href="{{ path('app_account') }}">Compte client</a>
                <a class="dropdown-item" href="{{ path('updateUserAdmin', {'id': app.user.id}) }}">Editer mon profil</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
            </div>
        </li>
    </ul>
</nav>
<div id="layoutSidenav">
    <div id="layoutSidenav_nav">
        <nav class="sb-sidenav accordion sb-sidenav-light" id="sidenavAccordion">
            <div class="sb-sidenav-menu">
                <div class="nav">
                    <a class="nav-link" href="{{ path('homeAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
                        Tableau de bord
                    </a>
                    <div class="sb-sidenav-menu-heading">Gestion des contenus</div>
                    <a class="nav-link" href="{{ path('teamAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-users"></i></div>
                        Equipe
                    </a>
                    <a class="nav-link" href="{{ path('postsAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="far fa-newspaper"></i></div>
                        Articles
                    </a>
                    <a class="nav-link" href="{{ path('eventsAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-praying-hands"></i></div>
                        Évènements
                    </a>
                    <a class="nav-link" href="{{ path('opinionsAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-comments"></i></div>
                        Témoignages
                    </a>

                    <div class="sb-sidenav-menu-heading">Boutique</div>
                    <a class="nav-link" href="{{ path('onlineAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fab fa-youtube"></i></div>
                        Magie en ligne
                    </a>
                    <a class="nav-link" href="{{ path('programsAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-book"></i></div>
                        Programmes/Formations
                    </a>

                    <div class="sb-sidenav-menu-heading">Taxonomies</div>
                    <a class="nav-link" href="{{ path('authorsAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-user-tie"></i></div>
                        Auteurs
                    </a>
                    <a class="nav-link" href="{{ path('categoriesAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-tags"></i></div>
                        Catégories
                    </a>
                    <a class="nav-link" href="{{ path('keywordsAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-hashtag"></i></div>
                        Hashtags
                    </a>

                    <div class="sb-sidenav-menu-heading">E-shop</div>
                    <a class="nav-link" href="{{ path('usersAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-user"></i></div>
                        Utilisateurs
                    </a>
                    <a class="nav-link" href="{{ path('purchasesAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-shopping-bag"></i></div>
                        Commandes
                    </a>
                    <a class="nav-link" href="{{ path('promoCodeAdmin') }}">
                        <div class="sb-nav-link-icon"><i class="fas fa-gift"></i></div>
                        Codes promo
                    </a>
                </div>
                <div class="sb-sidenav-footer">
                    <div class="small">Connecté en tant que:</div>
                    {{ app.user.username }}
                </div>
        </nav>
    </div>
    <div id="layoutSidenav_content" class="mt-5">
        <main>
            <div class="container-fluid">
                {# FLASHBAG MESSAGE #}
                {% set success = app.flashes('success') %}
                {% if success is not empty %}
                    <div class="alert alert-fade alert-success">
                        {% for message in success %}
                            {{ message }}<br/>
                        {% endfor %}
                    </div>
                {% endif %}
                {% set info = app.flashes('info') %}
                {% if info is not empty %}
                    <div class="alert alert-fade alert-info">
                        {% for message in info %}
                            {{ message }}<br/>
                        {% endfor %}
                    </div>
                {% endif %}
                {% set error =  app.flashes('error')  %}
                {% if error is not empty %}
                    <div class="alert alert-fade alert-danger">
                        {% for message in error %}
                            {{ message }}<br/>
                        {% endfor %}
                    </div>
                {% endif %}
                {# END FLASHBAG MESSAGE #}
                {% block content %}{% endblock %}
            </div>
        </main>
        <footer class="py-4 bg-light mt-auto">
            <div class="container-fluid">
                <div class="d-flex align-items-center justify-content-between small">
                    <div class="text-muted">Copyright &copy; Comètes 2022</div>
                    <div>
                        <a href="#">Privacy Policy</a>
                        &middot;
                        <a href="#">Terms &amp; Conditions</a>
                    </div>
                </div>
            </div>
        </footer>
    </div>
</div>
<script src="https://code.jquery.com/jquery-3.5.1.min.js" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.8.0/Chart.min.js" crossorigin="anonymous"></script>
<script src="https://cdn.datatables.net/1.10.20/js/jquery.dataTables.min.js" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bs-custom-file-input/dist/bs-custom-file-input.min.js"></script>
<script src="https://cdn.tiny.cloud/1/gmv991ukb11kak0q2t2dr2vkuqvfk7tgpj0di5lg7o1za117/tinymce/5/tinymce.min.js" referrerpolicy="origin"></script>
<script>
    tinymce.init({
        selector: 'textarea',
        height: 300,
        plugins: 'lists code emoticons link autolink',
        toolbar: 'undo redo | styleselect | bold italic | ' +
            'alignleft aligncenter alignright alignjustify | ' +
            'outdent indent | numlist bullist | emoticons | link',
        emoticons_append: {
            custom_mind_explode: {
                keywords: ['brain', 'mind', 'explode', 'blown'],
                char: '🤯'
            }
        },
        content_style: 'body { font-family:Helvetica,Arial,sans-serif; font-size:14px }',
    });

</script>
<script>
    $(document).ready(function () {
        $('#dataTable').DataTable({
            "aaSorting": [],
            "language": {
                "url": "https://cdn.datatables.net/plug-ins/1.10.21/i18n/French.json"
            },
            drawCallback: function () {
                $('div.dataTables_filter input').addClass('form-control');
                $('.dataTables_paginate .previous').addClass('btn btn-light rounded-0');
                $('.dataTables_paginate .paginate_button').addClass('btn btn-light rounded-0');
                $('.dataTables_paginate .next').addClass('btn btn-light rounded-0');
                $('.dataTables_paginate .disabled').addClass('btn btn-light rounded-0 text-muted');
                $('.dataTables_paginate .current').addClass('btn bg-primary rounded-0 text-white');
            }
        });

var images = $('.img_checkbox')

       images.each(function(){
            var file=$( this ).data('flag');
            var assetBaseDir = "{{ asset('uploads/cover_img/') }}";
            var media=assetBaseDir + file;
            $('#image_choice > div').addClass('d-flex flex-wrap');
           $( this ).next('label').css('background', 'url("'+media+'")  no-repeat');
           $( this ).next('label').css('backgroundSize', 'contain');
           $( this ).next('label').css('backgroundPosition', 'center center');
           $( this ).next('label').html('');
           $( this ).change(function() {
               if(this.checked) {
                   $('label').css('border','none');
                  $(this).next('label').css('border', '3px solid grey');
               }
               $('#textbox1').val(this.checked);
           });
        });
    });
</script>
{% block customScripts %}{% endblock %}
{{ encore_entry_script_tags('admin') }}

</body>
</html>

