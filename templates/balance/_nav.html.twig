<section class="container-fluid pt-3 pb-3 background-dark-green rm-content-margin">
    <div class="container">
        <div class="row list justify-content-around flex-wrap">
            {% if 'sono' not in app.request.attributes.get('_route') %}
                <div class="item item-5 mb-5 d-flex flex-column m-lg-2">
                    <a class="text-center" href="{{ path('sonoConsult') }}">
                        <figure class="change-ratio">
                            <img class="mw-100 h-100" src="{{ asset('build/images/design_sonotherapie.jpeg') }}" alt="">
                        </figure>
                    </a>
                </div>
            {% endif %}
            {% if 'astro' not in app.request.attributes.get('_route') %}
                <div class="item item-5 mb-5 d-flex flex-column m-lg-2">
                    <a class="text-center" href="{{ path('astroConsult') }}">
                        <figure class="change-ratio">
                            <img class="mw-100 h-100" src="{{ asset('build/images/design_astrologie.jpeg') }}" alt="">
                        </figure>
                    </a>
                </div>
            {% endif %}
        </div>
    </div>
</section>