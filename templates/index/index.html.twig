{% extends 'base.html.twig' %}

{% block content %}
    {# PHOTOS CAROUSEL #}
<header class="container-fluid p-0">
    <div id="carouselExampleControls" class="carousel home slide" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="overlay"></div>
                <img class="d-block w-100 h-100" src="{{ asset('build/images/home_bg.jpeg') }}"
                     alt="Photo d'une main tenant un tournesol">
                <div class="carousel-caption m-auto d-flex flex-column align-items-center justify-content-center w-80 text-center"></div>
            </div>
{#            <div class="carousel-item ">#}
{#                <div class="overlay"></div>#}
{#                <img class="d-block w-100 h-100" src="{{ asset('build/images/20.jpg') }}"#}
{#                     alt="Photo d'une main avec des dessins en peinture">#}
{#                <div class="carousel-caption m-auto d-flex flex-column align-items-center justify-content-center w-80 text-center"></div>#}
{#            </div>#}

{#            <div class="carousel-item">#}
{#                <div class="overlay"></div>#}
{#                <img class="d-block w-100 h-100" src="{{ asset('build/images/103.jpg') }}"#}
{#                     alt="Photo d'une main tenant un tournesol">#}
{#                <div class="carousel-caption m-auto d-flex flex-column align-items-center justify-content-center w-80 text-center"></div>#}
{#            </div>#}
        </div>
{#                    <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">#}
{#                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>#}
{#                        <span class="sr-only">Previous</span>#}
{#                    </a>#}
{#                    <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">#}
{#                        <span class="carousel-control-next-icon" aria-hidden="true"></span>#}
{#                        <span class="sr-only">Next</span>#}
{#                    </a>#}
    </div>
</header>
{# END PHOTOS CAROUSEL #}

  {# EXPERIENCE #}
<section class="container py-5  mt-100   pt-md-3 pt-lg-5" id="home_title">
    <div class="row justify-content-center align-items-center position-relative">
        <img class="col-12 col-md-10  col-lg-8 col-xl-6" src="{{ asset('build/images/motifs/demisphere_orange.png') }}"
             alt="">
        <img id="wave" class="col-12" src="{{ asset('build/images/motifs/trace_intuitif_noir.png') }}" alt="">
        <h2 class="text-center col-8 ">L’astrologie comme <br/> voie d’éveil à soi</h2>
    </div>
    <div class="row pt-5">
        <p class="col-12 text-center big-text">Comètes vous propose d’embarquer dans un voyage initiatique autour de votre carte du ciel et des fondamentaux de l’astrologie holistique.</p>

        <p class="col-12 text-center big-text"> Une aventure à la rencontre des énergies du zodiaque, afin de décrypter avec bienveillance et conscience, les messages des astres.</p>

        <p class="col-12 text-center big-text">Ensemble, nous explorerons une méthode de lecture empreinte de l’astrologie humaniste et karmique, qui se concentre sur le voyage de l’âme et la meilleure manière de l’unir à notre personnalité.</p>

        <p class="col-12 text-center big-text">Elle est rattachée à l’astrologie tropicale, occidentale.</p>

    </div>
    <div class="row">
        <img id="griffe" class="col-4 col-md-3 offset-xl-1 col-xl-2"
             src="{{ asset('build/images/motifs/trace_intuitif_2.png') }}" alt="">

    </div>

</section>
{# END EXEPERIENCE #}

    {# Type of offer #}
<section class="container  py-5 ">
    <div class="row text-center justify-content-around">
        <div class="col-12 col-sm-8  col-md-3 my-3 ">
            <a href="{{ path('astroConsult') }}">
                <img loading="lazy" class="opacity_hover col-12 p-0"
                     src="{{ asset('build/images/vignettes/1.jpg') }}" alt="">
            </a>
        </div>
        <div class="col-12 col-sm-8  col-md-3 my-3">
            <a href="{{ path('programOnline', { 'slug': 'cometes' }) }}">
                <img loading="lazy" class="opacity_hover col-12 p-0"
                     src="{{ asset('build/images/vignettes/cometes.jpg') }}" alt="">
            </a>
        </div>

    </div>
</section>
{# End type of offer #}





    {# THREE NEXT EVENTS #}
    {% if events is not empty %}
<section class="container-fluid p-0">
    <section class="container-fluid p-0">
        <header class="py-4 background-orange text-center col-12">
            <h2 class="m-0 text-uppercase">Évènements à venir</h2>
        </header>
        <div class="container py-5">
            <div class="row list justify-content-around flex-wrap text-center align-items-start ">
                {% for event in events %}
                    <article class="item col-12 col-md-6 col-lg-4 d-flex flex-column align-items-center">
                        <a class="d-flex align-items-center text-center"
                           href="{{ path("singleEvent", { 'slug': event.slug }) }}">
                            <figure>
                                <div class="effect"></div>
                                <img loading="lazy" src="{{ asset('uploads/cover_img/'~event.img.url) }}"
                                     alt="{{ event.img.alt }}">
                            </figure>
                        </a>
                        <div class="infos mt-3">
                            <h2 class="d-initial">
                                <a href="{{ path("singleEvent", { 'slug': event.slug }) }}">{{ event.title | raw }}</a>
                            </h2>
                            <p>{% if event.startDate | date('Ymd') != event.endDate | date('Ymd') %}
                                    <strong>Du {{ event.startDate | localizeddate('none', 'none', null, null, 'EEEE d MMMM') }}
                                        au {{ event.endDate | localizeddate('none', 'none', null, null, 'EEEE d MMMM YYYY') }}</strong>
                                {% else %}
                                    <strong>{{ event.startDate | localizeddate('none', 'none', null, null, 'EEEE d MMMM YYYY') }}</strong>
                                {% endif %}

                            </p>
                        </div>
                    </article>
                {% endfor %}

            </div>
{#            <div class="row px-md-5 px-xl-0">#}
{#                <p class="col-12 text-center big-text">"Si vous êtes une femme et que vous osez regarder à l’intérieur#}
{#                    de#}

{#                    vous-même, alors vous êtes une sorcière."</p>#}

{#                <p class="col-12 text-center big-text">Mona Chollet</p>#}

{#                <p class="col-12 text-center big-text"><em>Sorcières : La puissance invaincue des femmes</em></p>#}
{#            </div>#}
{#            <div class="row position-relative justify-content-center pt-5">#}
{#                <img id="orange-circle" class="col-4 col-md-3 col-xl-2"#}
{#                     src="{{ asset('build/images/motifs/rond_orange.png') }}" alt="">#}
{#                <img id="flower" class="col-4 col-md-3 col-xl-2 position-absolute"#}
{#                     src="{{ asset('build/images/motifs/Petiteplante_noir.png') }}" alt="">#}
{#            </div>#}
        </div>
    </section>
    {% endif %}
    {# END THREE NEXT EVENTS #}

    {# LAST PRESS #}
    <section class="container-fluid p-0 py-5">
        <header class="py-4 background-orange text-center col-12">
            <h2 class="m-0 text-uppercase">Comètes dans la presse</h2>
        </header>
        <div class="container pt-5">
            <div class="row list full-height-list justify-content-around flex-wrap align-items-start">
                {% for article in press %}
                    <article class="item col-12 col-sm-6 col-lg-4 mb-3 d-flex flex-column align-items-center">
                        <a class="d-flex align-items-center text-center "
                           href="{{ path('blogArticle', { 'slug': article.slug }) }}">
                            <figure>
                                <div class="effect"></div>
                                <img src="{{ asset('uploads/cover_img/'~article.img.url) }}"
                                     alt="{{ article.img.alt }}">
                            </figure>
                        </a>
                        <div class="infos mt-3">
                            <p>
                                <a href="{{ path('blogCategory', { 'slug': article.category.slug }) }}">{{ article.category.wording }}</a>
                            </p>
                            <h2 class="d-initial"><a
                                        href="{{ path('blogArticle', { 'slug': article.slug }) }}">{{ article.title }}</a>
                            </h2>
                        </div>
                    </article>
                {% endfor %}
            </div>

        </div>
    </section>
    {# END LAST PRESS #}

    {% include 'index/_social.html.twig' %}

    {% endblock %}
